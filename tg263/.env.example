# Configuração do ambiente
# Copie este arquivo para .env e ajuste conforme necessário

# Provider padrão (tgi para NVIDIA P40, vllm para GPUs mais novas)
PROVIDER=tgi

# Threshold para classificação TG-263 (0.0-1.0)
ABSTAIN_SIM_THRESHOLD=0.85

# Máximo de labels retornados no top-k
MAX_LABELS=5
